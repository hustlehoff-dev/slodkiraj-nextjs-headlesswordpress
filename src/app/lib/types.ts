export type Section = {
  id: string;
  title: string;
  content: string;
};
export type SliderImage = {
  id: string;
  sourceUrl: string;
  altText: string;
};
export type Project = {
  id: string;
  sourceUrl: string;
  altText: string;
};
